FROM rockylinux:9

LABEL org.opencontainers.image.title="minimax-hub-linux rpm builder"
LABEL org.opencontainers.image.description="Optional local RPM build environment for MiniMax Hub Linux release packaging."

RUN dnf install -y rpm-build rpm cpio rsync findutils tar \
  && dnf clean all

WORKDIR /work

CMD ["bash"]
